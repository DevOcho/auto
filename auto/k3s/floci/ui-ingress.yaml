apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: floci-ui
spec:
  rules:
    - host: floci-ui.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: floci-ui
                port:
                  number: 4500
